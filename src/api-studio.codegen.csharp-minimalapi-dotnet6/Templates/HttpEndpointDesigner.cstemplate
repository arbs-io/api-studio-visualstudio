namespace {{TOKEN_OAS_PROJECTNAME}}.{{TOKEN_OAS_NAMESPACE}}
{
    public partial class {{TOKEN_OAS_CLASS_NAME}} : ICarterModule
    {
        public void AddRoutes(IEndpointRouteBuilder app)
        {
            app.Map{{TOKEN_OAS_HTTP_VERB}}("/{{TOKEN_OAS_HTTP_URI}}", (Microsoft.AspNetCore.Http.HttpContext c) => RunAsync(c));
        }
    }
}